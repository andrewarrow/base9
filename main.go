package main

import (
	"base9/nonary"
	"fmt"
)

func main() {
	thing := nonary.NewInt(0)
	thing = thing.Add(1)
	fmt.Println(thing)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	fmt.Println(thing)
	thing = thing.Add(1)
	fmt.Println(thing)
	thing = thing.Add(1)
	fmt.Println(thing)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	thing = thing.Add(1)
	fmt.Println(thing)
}
